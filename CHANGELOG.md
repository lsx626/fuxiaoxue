# 更新日志

本文件记录每个发布版本的用户可见变化。发布说明以对应 GitHub Release 为准，
本文件是简表；实现细节、根因与不可回退约束见 `AGENTS.md`。

## v1.0.12（Android 1.0.12 / 桌面 1.0.8）

**Android**

- 预览渲染：补渲染 **母版/版式里的非占位装饰图形**（校徽、色带、装饰线），此前只画幻灯片自身形状，页面显得"缺件"。
- 预览渲染：**渐变填充**的形状不再整块消失（用各色标加权平均色兜底）；主题色应用 tint/shade 变换。
- 字符显示：新增文本清洗，处理 PPT 软换行 `\u000B`、OOXML 字面转义 `_x000B_` 与控制字符；文本/CSV 预览在 UTF-8 解码异常时回退 **GB18030**。
- 同步 404：下载链接过期（404/403）时重新获取签名 URL 并重试；课程未启用的标签页 404 静默处理并记住，后续跳过。
- 同步速度：文件内容下载改为 3 路并发（元数据请求仍串行遵守限流）。
- 安全收窄：移除未使用的存储/前台服务/唤醒锁权限，关闭 `usesCleartextTraffic`，FileProvider 从整块外部存储收窄到应用自己的目录。

**桌面**

- 页面归档改为**稳定命名 + 原子覆盖**：同一页面不再每轮生成 `(1)`、`(2)` 副本，并自动清理历史副本；正文未变不重写。
- "打开课程文件夹"改为复用同步引擎的路径清洗规则，含非法字符的课程名也能定位到正确目录。

## v1.0.11（Android 1.0.11）

- 同步可靠性：Link 分页、串行限流、429/`Retry-After`、指数退避；失败不再显示为"同步完成"。
- 可靠下载：`.part` 断点续传、长度校验、原子落盘、同名避让、登录页嗅探。
- 抓取能力对齐桌面端：目录树、模块、页面、作业、公告、大纲中的文件引用；按 Canvas 目录重建本地子目录。
- 数据库 schema v2 非破坏性迁移；修复**全新安装首次同步崩溃**。
- 修复 Android 上 PPT 填充与描边被静默丢弃（`java.awt` 桩补齐 `DrawPaint` 依赖）。
- 预览：形状/连接线/组合/图表占位卡、大文档降分辨率、解析进度与取消、跳页与重置缩放。
- 界面：同步失败横幅与重试、全量同步菜单、失败文件重试、设置页上次同步结果、关于页。

## v1.0.10（Android 1.0.10 / 桌面 1.0.7）

- Android 分页/限流/可靠下载/抓取对齐/数据库迁移/UI 状态与错误提示。
- 桌面：状态库写事务异常回滚、`login --method cookie` 回写配置、认证错误文案脱敏、退出等待线程结束、排除扩展名归一化。

## v1.0.9（Android 1.0.9）

- 大 PPT/PPTX 可解析（POI 记录上限按内存自适应 + 友好失败说明）。
- PPT 形状保真：自动形状（含无文字装饰形状）、连接线箭头、组合形状、旋转；图表/SmartArt/OLE/视频改为占位卡。
- 系统返回键、预览顶栏文件名、关于页、跳页与缩放复位。

## v1.0.8（Android 1.0.8）

- Word 完整页面：内嵌图片、跨页表格、逐段字符格式；修正 `.docx` 图片尺寸单位。

## v1.0.7（Android 1.0.7）

- 文件分享；Office 六格式应用内逐页渲染；PDF/Office 纵向连续滚动与双指缩放。

## v1.0.5 / v1.0.6

- 桌面同步删除保护、并发下载与路径安全；Android 应用内预览与界面重绘。
